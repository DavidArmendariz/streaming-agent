[project]
name = "streaming-agent-service"
version = "0.1.0"
description = "Default template for PDM package"
authors = [{ name = "Camilovelez1", email = "camilo95022@gmail.com" }]
dependencies = [
  "fastapi>=0.115.12",
  "uvicorn>=0.34.0",
  "python-dotenv>=1.1.0",
  "twilio>=9.5.2",
  "websockets==10.1",
]
requires-python = "==3.12.*"
readme = "README.md"
license = { text = "MIT" }


[tool.pdm]
distribution = false
